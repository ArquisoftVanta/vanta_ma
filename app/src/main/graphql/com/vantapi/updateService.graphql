mutation UpdateService($service: serviceInputFull!){
    updateService(service: $service){
        service_id
        user_id
        date_
        state_service
        vehicle_id
        service_value
        places
    }
}