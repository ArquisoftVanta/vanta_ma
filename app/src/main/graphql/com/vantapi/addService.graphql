mutation CreateService($service: serviceInput!){
    createService(service: $service){
        user_id
        date_
        state_service
        vehicle_id
        service_value
        places
    }
}