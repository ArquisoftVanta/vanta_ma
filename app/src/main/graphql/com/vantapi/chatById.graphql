query chatById($user_id: String!, $chat_id: String!){
    chatById(user_id: $user_id, chat_id: $chat_id){
        _id
        user1
        user2
        conversation{
            sender
            content
            createdAt
        }
    }
}