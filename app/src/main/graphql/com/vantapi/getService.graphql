query GetService($service_id: Int!){
    getService(service_id:$service_id){
        service_id
        user_id
        date_
        state_service
        vehicle_id
        service_value
        places
    }
}